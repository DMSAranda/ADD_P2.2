He utilizado un metodo en el que cuando el programa arranca, conecta con la base de datos test,
para crear la base de datos app 
y dentro la tabla usuario en la que carga al admin que no se puede editar y un usuario de demo,
he metido provincias y municipios con json.
A parte incluyo una carpeta SQL con las inserciones que serian necesarias en el fichero login.sql